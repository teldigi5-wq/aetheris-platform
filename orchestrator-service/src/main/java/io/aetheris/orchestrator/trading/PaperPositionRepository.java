package io.aetheris.orchestrator.trading;import org.springframework.data.jpa.repository.JpaRepository;import java.util.*;public interface PaperPositionRepository extends JpaRepository<PaperPositionEntity,UUID>{List<PaperPositionEntity> findTop100ByOrderByOpenedAtDesc();List<PaperPositionEntity> findByStatus(PaperPositionStatus status);boolean existsBySignalId(UUID signalId);}
