Aetheris has been installed into namespace {{ .Release.Namespace }}.

Check workloads:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get svc -n {{ .Release.Namespace }}

Open the dashboard locally:
  kubectl port-forward -n {{ .Release.Namespace }} svc/dashboard 3000:80

Then browse:
  http://localhost:3000

RabbitMQ management UI:
  kubectl port-forward -n {{ .Release.Namespace }} svc/rabbitmq 15672:15672

Scale a stateless service to demonstrate Kubernetes load balancing:
  kubectl scale deployment user-service -n {{ .Release.Namespace }} --replicas=2
  kubectl get pods -n {{ .Release.Namespace }} -l app=user-service -w

This chart uses development-only default secrets. Override them outside local development.
