# Aetheris v0.1.0-pre-pc — Release Candidate Notes

**Release class:** `REPOSITORY_PRE_PC`  
**Candidate status:** prepared, not yet published  
**Latest fully certified platform baseline before this documentation refresh:** `6203674f5ff1c867280f6f8005fa18fc2689e7b3`  
**Final release commit:** resolve from protected `main` at publication  
**Certified AI runtime checkpoint:** `68af39a1115a7330020c18b6e2cb601e66b8f22f`  
**Physical-machine status:** `BLOCKED_PENDING_HARDWARE`

This is the first formal pre-PC release-candidate narrative for the Aetheris platform after the AI-runtime extraction. It describes repository-side engineering maturity only. It does **not** claim that the complete system has been validated on the owner's target physical PC, activated in production, published to a container registry, or enabled for live-money execution.

## Repository topology

Aetheris is now intentionally split across two source owners:

- **Platform:** `teldigi5-wq/aetheris-platform`
- **AI runtime:** `teldigi5-wq/aetheris-ai-runtime`

The platform owns gateway, identity, user, audit, dashboard, deployment/integration contracts, platform observability assets and cross-repository certification references.

The AI runtime owns `orchestrator-service/`, `workstation-agent/`, `aetheris-reasoning/` and `aetheris-quant/`, including the runtime-side orchestration, browser/operator, reasoning, PC-care and trading-intelligence implementations and their applicable certification workflows.

The platform records `68af39a1115a7330020c18b6e2cb601e66b8f22f` as the certified runtime checkpoint. A newer runtime `main` commit is not automatically certified merely because it is newer or because documentation/governance CI passes. The runtime repository's current protected `main` may therefore be newer without changing this release's certified runtime checkpoint.

## Platform highlights

- Historical repository roadmap completed through **Stage 34 / 34**, retained for contract/history continuity rather than as the primary portfolio claim.
- Active GitHub ruleset protecting stable `main`, plus Stage 27 repository-governance checks for reviewed promotion.
- Post-extraction repository hygiene reduced the platform live branch set from 81 to five intentional refs: protected `main`, the synchronized canonical development line, and three preserved forensic/recovery refs.
- Java 21 / Spring Boot platform services with gateway, identity, user and audit boundaries.
- React + TypeScript dashboard and reviewer-friendly platform-only local demo path.
- PostgreSQL, Redis and RabbitMQ integration foundations.
- OpenTelemetry, Prometheus, Grafana, Loki and Tempo observability assets.
- Docker Compose platform-core and external-runtime integration contracts, plus Kubernetes/Helm deployment assets.
- Core-source-independence checks proving extracted AI-runtime source is absent from the platform repository.
- Dependency lockdown, reproducibility, release-hardening, CodeQL, platform evidence and compatibility controls.
- Portfolio, security, contribution, release-readiness and first-boot documentation aligned with the split-repository architecture.

## Certified AI-runtime checkpoint

The platform integrates only against an exact runtime revision recorded in `architecture/ai-runtime-certification-reference.json`.

For this candidate, the certified runtime checkpoint is:

```text
68af39a1115a7330020c18b6e2cb601e66b8f22f
```

Its platform-recorded evidence includes the complete migrated runtime regression, Stage 26 runtime safety certification and image-provenance certification. Stage 28 PC-care and Stage 29 trading-intelligence certification are runtime-owned responsibilities and must be evaluated in `teldigi5-wq/aetheris-ai-runtime`, not inferred from platform-local source that no longer exists.

The runtime tag `runtime-68af39a1115a7330020c18b6e2cb601e66b8f22f` identifies that checkpoint. The platform record does **not** claim that its image has been published to a registry.

## Safety and governance boundaries

The following remain non-negotiable for this release class:

- deterministic owner policy outranks model recommendations;
- `ALLOW` means eligible to execute, not proof that execution occurred;
- success requires verification evidence or an explicit `UNVERIFIED` result;
- emergency precedence remains `STOP > TAKE_CONTROL > PAUSE > NORMAL`;
- protected/private data and billable fallbacks remain constrained by owner policy;
- browser mutations and consequential external actions remain approval/evidence gated;
- live-money execution, withdrawals and transfers remain outside the default trusted AI path;
- hosted CI is repository evidence, not physical-PC validation;
- `BLOCKED_PENDING_HARDWARE` remains the physical-machine state until real target-machine evidence exists.

## Repository validation expected at publication

The exact platform release commit must satisfy the current platform release gate in `docs/release-readiness.md`, including:

- Platform Build on the exact candidate commit;
- CodeQL for **Core Java** and JavaScript/TypeScript;
- core-source-independence and release-hardening evidence;
- dependency-lockdown and two-pass reproducibility checks;
- Stage 25 platform + external-runtime readiness when applicable;
- Stage 26 platform evidence certification when applicable;
- Stage 27 reviewed promotion into `main`;
- portfolio/release evidence checks when triggered;
- a consistent `architecture/ai-runtime-certification-reference.json` and external-runtime integration contract;
- required runtime-owned certification evidence for the referenced runtime checkpoint.

Stage 28 PC-care and Stage 29 trading-intelligence are **not** platform-local release jobs after extraction. They belong to the AI-runtime repository.

## Administrative ruleset gate — complete

The active repository ruleset **`Protect stable main`** has been aligned with the post-extraction platform check names. It targets the default branch, blocks deletion and non-fast-forward updates, requires pull-request promotion with review-thread resolution, and enforces strict/up-to-date required status checks for the current platform gates.

The stale pre-extraction contexts were removed rather than replaced with fake compatibility jobs. This administrative governance item is therefore no longer a release blocker. The required-check set must still be re-read immediately before publication so the final release commit is evaluated against the live ruleset rather than a copied historical list.

## Known limitations

This candidate intentionally does not claim validation of:

- WSL2 or virtualization on the final owner PC;
- Docker Desktop/runtime behavior on that machine;
- RTX GPU acceleration or local-model throughput;
- thermal, storage or long-duration resource behavior;
- microphone/speaker/voice hardware;
- ChromeDriver/EdgeDriver installation or browser launch behavior on the real machine;
- authenticated browser profiles, cookies or session persistence on the real machine;
- LinkedIn-, Vercel-, Facebook-, Instagram- or other site-specific workflow stability on the owner PC;
- unrestricted privileged host execution;
- production deployment readiness outside the declared repository scope;
- registry publication of the certified runtime image;
- live-money trading execution.

Physical-PC validation remains pending.

## Licensing status

The repository does not currently have a GitHub-recognized software license. No license is selected by this release-candidate document. The owner must make that legal/permission decision deliberately before representing the project as generally licensed for third-party reuse.

Until that decision is made, public repository visibility must not be described as granting open-source reuse rights.

## Intended audience

This pre-PC candidate is appropriate for:

- architecture and code review;
- portfolio/interview demonstration;
- repository-side local reproduction;
- CI, security and governance review;
- preparation for eventual physical first-boot validation.

It should **not** be described as a physically validated or production-ready release.

## Publication checklist

Before creating the GitHub release:

1. merge final release-alignment changes through the reviewed `release/*` path into protected `main`;
2. record the exact final platform `main` SHA in the published release notes;
3. keep the certified runtime checkpoint pinned to `68af39a1115a7330020c18b6e2cb601e66b8f22f` unless a separate runtime recertification and reviewed platform reference update occurs;
4. confirm all applicable platform checks are green on the exact release commit;
5. confirm required runtime-owned certification evidence exists for the pinned runtime checkpoint;
6. confirm the active GitHub ruleset still uses the intended post-extraction platform required checks;
7. confirm `README.md` still preserves `Stage 34 / 34`, `BLOCKED_PENDING_HARDWARE` and physical-validation-pending truth markers;
8. decide the software-license strategy or explicitly retain the no-license state without claiming open-source reuse rights;
9. create an immutable `v0.1.0-pre-pc` tag on the exact protected-`main` release commit;
10. mark the GitHub release as a **pre-release** and preserve all truth boundaries above.

The canonical product/safety contract remains `docs/master-build-spec.md`; release gating is defined in `docs/release-readiness.md`.
